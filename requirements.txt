# Core dependencies
# Note: The main Rust extension is built with maturin, so no Python dependencies are required
# This file is kept for compatibility with setup.py

# Development dependencies (install with: pip install -e .[dev])
# pytest>=6.0
# pytest-benchmark>=3.0
# psutil>=5.8

